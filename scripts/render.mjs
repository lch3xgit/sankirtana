console.log(`Browser PNG export is the canonical MVP path for now.

Open the site locally and use Download PNG from the generator.

The shared browser renderer lives in src/render/. A future pass can wire this
script to the same renderer for local CLI exports without changing the sacred
template geometry.`);
